# 基础数据管理优化方案

> 基于现有系统数据库结构分析、五类基础数据（车型/科目/考场/费用标准/特种车辆）的现状评估，提出改进方案。

---

## 一、现状总览

对现有系统 12 张表中与基础数据相关的模块逐一评估：

| 数据域 | 涉及表/字段 | 现状评级 | 核心问题 |
|--------|-----------|---------|---------|
| 车型 | `license_config` | ★★★☆ 基本完整 | 缺少考试模式区分（小汽车/特种车辆） |
| 科目 | 散落在 5 张表的 `TINYINT` 字段 | ★★☆☆ 亟需改进 | 无独立字典表，科目 1-4 硬编码，无法扩展 |
| 考场 | `exam_session.location` VARCHAR 字段 | ★☆☆☆ 需重构 | 无独立考场表，地点信息冗余、无法独立管理 |
| 费用标准 | `fee_standard` | ★★★★ 设计良好 | subject=NULL 表示套餐是亮点，基本不需改动 |
| 特种车辆 | 不存在 | ☆☆☆☆ 未实现 | 全新需求，需设计考试模式和数据模型 |
| 系统配置 | `config` 表存在但无 Java 代码对接 | ★☆☆☆ 僵尸表 | 7 条配置已入库但从未被读取 |

---

## 二、各域详细改进方案

### 2.1 车型（`license_config`）— 小改

**改动思路**：不改表结构，只加 1 个字段，扩展数据含义。

```sql
ALTER TABLE `license_config`
  ADD COLUMN `exam_mode` TINYINT NOT NULL DEFAULT 1
    COMMENT '考试模式: 1-小汽车(科目一~四), 2-特种车辆(理论+实操)',
  ADD COLUMN `pass_score` TINYINT UNSIGNED NOT NULL DEFAULT 90
    COMMENT '该科目及格分数',
  ADD COLUMN `cert_name` VARCHAR(50) DEFAULT NULL
    COMMENT '获证名称(仅特种车辆使用, 如"叉车操作证")';
```

**说明**：
- `exam_mode=1` 沿用现有科目一~四的逻辑，零影响
- `exam_mode=2` 表示特种车辆模式，`subject=1`=理论, `subject=2`=实操
- `pass_score` 解耦现有代码中硬编码的 `≥90` 判断
- 现有数据 `exam_mode` 默认 1，`pass_score` 默认 90，完全向后兼容

**现有数据迁移示例**：

```sql
-- 小汽车数据不受影响（exam_mode=1 是默认值）
-- 新增特种车辆配置：
INSERT IGNORE INTO `license_config` 
  (`license_type`, `subject`, `required_hours`, `exam_items`, `description`, `sort_order`, `exam_mode`, `pass_score`, `cert_name`)
VALUES
('N1', 1, 0,     '安全法规,叉车基础知识',                               '叉车理论考试', 1, 2, 70, '叉车操作证'),
('N1', 2, 20,    '起步,货叉装卸,倒车入库,停放',                          '叉车实操考试', 2, 2, 80, NULL),
('N2', 1, 0,     '安全法规,挖掘机基础知识',                              '挖掘机理论考试', 1, 2, 70, '挖掘机操作证'),
('N2', 2, 25,    '挖沟,平整,装车,上下板车,行走',                        '挖掘机实操考试', 2, 2, 80, NULL),
('N3', 1, 0,     '安全法规,压路机基础知识',                              '压路机理论考试', 1, 2, 70, '压路机操作证'),
('N3', 2, 20,    '起步,压实作业,转向,掉头,停放',                         '压路机实操考试', 2, 2, 80, NULL);
```

**涉及代码改动**：
- `ExamRegistrationController.enterScore()`：从硬编码 `score >= 90` 改为查 `license_config.pass_score`
- `ProgressServiceImpl`：增加 `exam_mode=2` 的分支判断（2 科模式 + 获证状态）
- `LicenseConfigController`：按车型查询时也返回 `exam_mode` 给前端判断展示

---

### 2.2 科目字典表 — 新建

**改动思路**：新建 `subject` 表作为科目字典，现有 5 张表的 `TINYINT` 字段改为外键关联。

```sql
CREATE TABLE `subject` (
    `id` TINYINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '科目ID（保持1-4兼容现有数据）',
    `name` VARCHAR(50) NOT NULL COMMENT '科目名称（如"科目一 道路交通安全法律、法规和相关知识"）',
    `short_name` VARCHAR(20) NOT NULL COMMENT '科目简称（如"科目一"、"理论"、"实操"）',
    `exam_mode` TINYINT NOT NULL DEFAULT 1 COMMENT '适用考试模式: 1-小汽车, 2-特种车辆, 3-全部',
    `sort_order` TINYINT UNSIGNED NOT NULL DEFAULT 0 COMMENT '排序',
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='考试科目字典表';

-- 初始化数据
INSERT INTO `subject` (`id`, `name`, `short_name`, `exam_mode`, `sort_order`) VALUES
(1, '科目一 道路交通安全法律、法规和相关知识', '科目一', 1, 1),
(2, '科目二 场地驾驶技能',                     '科目二', 1, 2),
(3, '科目三 道路驾驶技能',                     '科目三', 1, 3),
(4, '科目四 安全文明驾驶常识',                 '科目四', 1, 4);
```

**涉及表结构改动**（可选，低优先级）：

| 表 | 字段 | 改动方式 |
|----|------|---------|
| `exam_session.subject` | TINYINT → TINYINT | 值域不变，加外键约束（可选） |
| `exam_registration.subject` | TINYINT | 同上 |
| `training_record.subject_type` | TINYINT | 同上 |
| `license_config.subject` | TINYINT | 同上 |
| `fee_standard.subject` | TINYINT | 同上 |

**说明**：科目 1-4 是国标，几乎不会变。此表主要作用是：
1. 给前端下拉菜单提供数据源（免硬编码）
2. 支持特种车辆新增"理论/实操"科目类型
3. 为未来可能的科目五预留扩展点

**优先级**：建议放到 P2（低优先级）或配合特种车辆需求一起做。

---

### 2.3 考场表 — 从 `exam_session` 中抽离

**改动思路**：新建 `exam_venue` 表，`exam_session` 通过 `venue_id` 关联。

```sql
CREATE TABLE `exam_venue` (
    `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
    `name` VARCHAR(100) NOT NULL COMMENT '考场名称（如"南岸区车管所"）',
    `address` VARCHAR(200) DEFAULT NULL COMMENT '详细地址',
    `contact_phone` VARCHAR(20) DEFAULT NULL COMMENT '联系电话',
    `capacity` INT UNSIGNED DEFAULT NULL COMMENT '容纳人数',
    `facilities` VARCHAR(500) DEFAULT NULL COMMENT '设施设备说明',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态: 1-启用, 0-停用',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    `is_deleted` TINYINT(1) NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`),
    KEY `idx_status` (`status`),
    KEY `idx_is_deleted` (`is_deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='考场信息表';
```

**`exam_session` 表改动**：
```sql
ALTER TABLE `exam_session`
  ADD COLUMN `venue_id` INT UNSIGNED DEFAULT NULL COMMENT '关联考场ID' AFTER `location`,
  ADD KEY `idx_venue_id` (`venue_id`);

-- 迁移现有数据：将 location 中的考场名称转为考场记录
-- （迁移脚本略，根据实际情况编写）
```

**效果**：
- 考场信息统一管理，修改一次到处生效
- 一个考场可用于多个场次，支持查看考场使用频率统计
- `exam_session` 不再冗余存储地址、电话等信息

---

### 2.4 费用标准（`fee_standard`）— 基本不动

现有设计已经较好，只建议加一个按车型分组的聚合查询：

```java
// FeeStandardServiceImpl 新增方法
List<FeeStandardGroupVO> listGroupedByLicenseType();
```

返回前端按车型分组的套餐+单科费用，方便展示价格对比。

---

### 2.5 特种车辆 — 全新增量模块

**核心设计原则**：不改现有表结构，只做增量扩展。

#### 数据层
- 车型配置直接写入 `license_config`（见 2.1 节），`exam_mode=2`
- 考试报名走独立流程，不走 `exam_session→exam_registration` 的场次配额体系

#### 业务层 — 新建 `SpecialExamController` + `SpecialExamService`

```
特种车辆考试流程：

  学员报名特种车型
       ↓
  考理论 (subject=1)
       ↓  pass? → 否 → 补考理论，次数不限
       ↓  是
  考实操 (subject=2)
       ↓  pass? → 否 → 补考实操，次数不限
       ↓  是
   获证（状态标记为"已获证"，记录证书编号）
```

**关键差异**：

| 环节 | 小汽车（现有流程） | 特种车辆（新流程） |
|------|------------------|------------------|
| 考试组织 | 管理员发布场次→学员报名→审核→扣名额 | 学员报名→直接安排考试，无需场次系统 |
| 补考规则 | 科二/科三最多补考5次 | 理论/实操分别补考，不限次数（或按政策） |
| 证书 | 无 | 双科通过后发操作证 |

**新增表**（轻量，只记考试成绩）：

```sql
CREATE TABLE `special_exam_record` (
    `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
    `student_id` INT UNSIGNED NOT NULL COMMENT '学员 user_id',
    `license_type` VARCHAR(10) NOT NULL COMMENT '车型 N1/N2...',
    `subject` TINYINT NOT NULL COMMENT '1-理论, 2-实操',
    `score` TINYINT UNSIGNED DEFAULT NULL COMMENT '成绩',
    `pass_status` TINYINT DEFAULT NULL COMMENT '是否合格: 0-不合格, 1-合格',
    `retake_count` TINYINT UNSIGNED NOT NULL DEFAULT 0 COMMENT '该科目补考次数',
    `exam_date` DATETIME DEFAULT NULL COMMENT '考试时间',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    `is_deleted` TINYINT(1) NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`),
    KEY `idx_student` (`student_id`),
    KEY `idx_student_license` (`student_id`, `license_type`),
    KEY `idx_is_deleted` (`is_deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='特种车辆考试记录表';
```

**注意**：不用复用 `exam_registration` 的原因是它强依赖 `session_id`（外键到 exam_session），且包含名额扣减逻辑，与特种车辆场景不匹配。独立的轻量表更干净。

#### ProgressServiceImpl 扩展

在现有进度逻辑中增加 `exam_mode=2` 分支：

```
exam_mode=1: 沿用现有科目链（科一→科二→科三→科四）
exam_mode=2: 简化链（理论→实操→已获证）
  - 理论未考/未过 → status=learning
  - 理论已过 → status=ready（可考实操）
  - 实操已过 → status=passed + isCertified=true + certName
```

---

### 2.6 系统配置表对接 — 激活僵尸表 `config`

`config` 表已有 7 条配置但 Java 代码从未读取。建议新建 `SystemConfigService`：

```java
@Service
public class SystemConfigService {

    @Autowired
    private ConfigMapper configMapper;  // 需要新建 ConfigMapper + Config 实体

    public String getConfig(String key) {
        // 查 config 表获取配置值
    }

    public Integer getInt(String key) {
        return Integer.parseInt(getConfig(key));
    }
}
```

**优先对接的配置项**：

| config_key | 用途 | 当前硬编码位置 |
|-----------|------|--------------|
| `hours_required_subject1` | 科目一学时要求 | license_config 已有，但 config 表中也有 |
| `cancel_advance_hours` | 取消约课需提前小时数 | 未使用 |
| `max_no_show_count` | 爽约次数上限 | 未使用 |
| `no_show_punish_days` | 爽约后禁止约课天数 | 未使用 |

---

## 三、优先级与实施路径

```
P0 ─ 立刻做（低投入、高收益）
  ├── 2.1 license_config 加 exam_mode / pass_score 字段
  ├── 2.5 新增 special_exam_record 表 + SpecialExamController
  └── 2.6 激活 config 表（先对接爽约惩罚逻辑）

P1 ─ 本周做（中等投入、中等收益）
  ├── 2.3 考场表抽离（exam_venue + exam_session.venue_id）
  └── 改造 enterScore 读取 pass_score 而非硬编码 90

P2 ─ 后续迭代（灵活安排）
  ├── 2.2 科目字典表（若前端需要动态科目列表再做）
  ├── 2.4 费用标准分组查询接口
  └── 2.1 ProgressServiceImpl 支持特种车辆进度展示
```

**影响范围总结**：

| 改动 | 新增文件 | 修改文件 | 不动的文件 |
|------|---------|---------|-----------|
| 车型扩展 | 无 | `license_config` 表 + `ExamRegistrationController` + `ProgressServiceImpl` | 全部 Mapper、实体 |
| 特种车辆 | `SpecialExamController` + `SpecialExamService` + `SpecialExamServiceImpl` | `ProgressServiceImpl` | 全部现有考试代码 |
| 考场抽离 | `ExamVenue` 实体 + Mapper + Service | `exam_session` 表 + `ExamSession` 实体 | 现有业务逻辑 |
| 系统配置 | `ConfigMapper` + `SystemConfigService` | 新增调用方 | 现有表和代码 |
